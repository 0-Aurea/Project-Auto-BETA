name: Continuous Integration

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Install dependencies
        run: npm install

      - name: Lint code
        run: npm run lint

      - name: Run tests
        run: npm run test

      - name: Test JS rewriting functionality
        run: |
          npm run test-js-rewriting

      - name: Automated fetch tests
        run: npm run fetch-tests

      - name: Test WebSocket functionality
        run: |
          npm run test-websocket

      - name: Test WebRTC functionality
        run: |
          npm run test-webrtc

      - name: Build and package
        run: npm run build

      - name: Docker build and test
        run: |
          docker build -t nexus-proxy .
          docker run -p 8080:8080 nexus-proxy &
          sleep 10
          curl http://localhost:8080 > /dev/null
          docker stop $(docker ps -q)

  js-rewriting-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Install dependencies
        run: npm install

      - name: Run JS rewriting tests
        run: |
          npm run test-js-rewriting -- --coverage

  codeql:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Initialize CodeQL
        uses: github/codeql-action@v2
        with:
          languages: javascript

  security-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run security audit
        run: npm audit --json > audit.json 

      - name: Upload security audit results
        uses: actions/upload-artifact@v3
        with:
          name: audit.json
          path: audit.json 

  cache-hits:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Get cache hits
        run: |
          echo "Cache hits: ${{ steps.cache-hits.outputs.cache-hits }}"